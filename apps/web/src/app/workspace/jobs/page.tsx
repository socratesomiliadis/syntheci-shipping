import Link from "next/link";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { ensureDefaultWorkspace } from "@syntheci/db";
import { loadOperationalJobs } from "@/lib/voyage-workflows";

export const dynamic = "force-dynamic";

type SearchParams = Promise<Record<string, string | string[] | undefined>>;

export default async function JobsPage({ searchParams }: { searchParams: SearchParams }) {
  const params = await searchParams;
  const status = getParam(params.status);
  const workspaceId = await ensureDefaultWorkspace();
  const jobs = await loadOperationalJobs(workspaceId, status);

  return (
    <div className="space-y-5">
      <section className="rounded-lg border border-slate-200 bg-white p-5">
        <h1 className="text-xl font-semibold text-slate-950">Operational Jobs</h1>
        <p className="mt-1 text-sm text-slate-500">Voyage work items generated by missing-document, risk, claims, payment, and watchlist workflows.</p>
        <div className="mt-4 flex flex-wrap gap-2">
          {["open", "in_progress", "resolved", "dismissed"].map((item) => (
            <Button asChild key={item} size="sm" variant={status === item ? "default" : "outline"}>
              <Link href={`/workspace/jobs?status=${item}`}>{formatLabel(item)}</Link>
            </Button>
          ))}
          <Button asChild size="sm" variant={!status ? "default" : "outline"}>
            <Link href="/workspace/jobs">All</Link>
          </Button>
        </div>
      </section>

      <section className="overflow-hidden rounded-lg border border-slate-200 bg-white">
        <Table>
          <TableHeader>
            <TableRow>
              <TableHead>Job</TableHead>
              <TableHead>Voyage</TableHead>
              <TableHead>Type</TableHead>
              <TableHead>Priority</TableHead>
              <TableHead>Status</TableHead>
            </TableRow>
          </TableHeader>
          <TableBody>
            {jobs.map((job) => (
              <TableRow key={job.id}>
                <TableCell>
                  <Link className="font-medium text-slate-950 hover:text-blue-700" href={`/workspace/jobs/${job.id}`}>
                    {job.title}
                  </Link>
                  <div className="mt-1 line-clamp-1 text-xs text-slate-500">{job.summary}</div>
                </TableCell>
                <TableCell>
                  <Link className="text-sm font-medium text-blue-700" href={`/workspace/voyages/${job.voyageId}`}>
                    {job.voyageId}
                  </Link>
                </TableCell>
                <TableCell>{formatLabel(job.jobType)}</TableCell>
                <TableCell>
                  <Badge variant="outline">{job.priority}</Badge>
                </TableCell>
                <TableCell>
                  <Badge variant="outline">{formatLabel(job.status)}</Badge>
                </TableCell>
              </TableRow>
            ))}
          </TableBody>
        </Table>
      </section>
    </div>
  );
}

function getParam(value: string | string[] | undefined) {
  return Array.isArray(value) ? value[0] : value;
}

function formatLabel(value: string) {
  return value.replace(/[_-]+/g, " ").replace(/\b\w/g, (letter) => letter.toUpperCase());
}
